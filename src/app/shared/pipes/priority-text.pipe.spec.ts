import { PriorityTextPipe } from './priority-text.pipe';

describe('PriorityTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityTextPipe();
    expect(pipe).toBeTruthy();
  });
});
